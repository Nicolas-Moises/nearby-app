import { StyleSheet } from "react-native";

export const s = StyleSheet.create({
  container: {
    maxHeight: 36,
    height: 36,
    zIndex: 1,
    top: 64,
    position: 'absolute',
  },
  content: {
    gap: 8,
    paddingHorizontal: 24,
  }
})